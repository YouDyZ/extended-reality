import * as alt from 'alt';
import chat from 'chat';
import mongoose from 'mongoose';

const test = {
    garage: {
        type: String,
    },
    leiter: {
        type: String,
    },
    funktion: {
        type: [String],
    },
};
